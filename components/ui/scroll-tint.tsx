"use client";

/**
 * ScrollTint — REMOVED in Session 23.
 *
 * The page-wide radial gold tint that shifted on scroll was a gimmick that
 * fought the editorial look. Removed. Kept as a no-op stub so any lingering
 * imports resolve cleanly. Safe to delete once no consumer imports it.
 */
export function ScrollTint() {
  return null;
}
