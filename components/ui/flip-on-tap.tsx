/**
 * Deprecated (Session 23): Flip-card UX removed site-wide per luxury design directive.
 *
 * Kept as a no-op to preserve any existing imports and avoid a hard breakage.
 * Remove this file entirely once all imports are verified cleaned up.
 */
export function FlipOnTap() {
  return null;
}
